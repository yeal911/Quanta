<Window x:Class="Quanta.Views.RecordingOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Recording"
        Width="185" Height="155"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        ResizeMode="NoResize"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Loaded="Window_Loaded">

    <!-- 根 Grid：包含加载层和主内容层 -->
    <Grid>
        <!-- 加载层：半透明背景 + 文字 -->
        <Border x:Name="LoadingOverlay" Background="#CC666666" CornerRadius="6" Height="25">
            <TextBlock x:Name="LoadingText"
                       Foreground="White"
                       FontSize="12"
                       FontFamily="微软雅黑"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Border>

        <!-- 主内容层：录音控制界面 (初始透明度为0，等待淡入) -->
        <StackPanel x:Name="MainContent" HorizontalAlignment="Stretch" VerticalAlignment="Center"
                    RenderTransformOrigin="0.5,0.5" Opacity="0">
            <StackPanel.RenderTransform>
                <TransformGroup>
                    <ScaleTransform x:Name="RootScale"/>
                    <TranslateTransform x:Name="RootTranslate"/>
                </TransformGroup>
            </StackPanel.RenderTransform>

        <!-- GIF 录制图标（居中，含 ScaleTransform 供图标过渡动画使用） -->
        <Image x:Name="RecordingIcon"
               Width="54" Height="54"
               HorizontalAlignment="Center"
               SnapsToDevicePixels="True"
               Stretch="Uniform"
               RenderOptions.BitmapScalingMode="Fant"
               RenderTransformOrigin="0.5,0.5"
               Margin="0,0,0,6">
            <Image.RenderTransform>
                <ScaleTransform x:Name="IconScaleTransform"/>
            </Image.RenderTransform>
        </Image>

        <!-- 统一控制面板：外层灰色半透明容器，宽度由内容决定（5×按钮自然撑开） -->
        <Border Background="#88707070" CornerRadius="6" Padding="4,4"
                HorizontalAlignment="Center">
            <StackPanel>

                <!-- 按钮容器：内层高透明背景 -->
                <Border Background="#55252525" CornerRadius="3" Padding="1,1" Margin="0,0,0,3"
                        HorizontalAlignment="Center" Width="125">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button x:Name="PauseButton" Width="24" Height="24" Padding="0" Margin="0.2,0,0.2,0"
                                FocusVisualStyle="{x:Null}" Click="PauseButton_Click" IsEnabled="False">
                            <Button.ToolTip><ToolTip x:Name="PauseTooltip"/></Button.ToolTip>
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="Transparent" HorizontalAlignment="Center" VerticalAlignment="Stretch">
                                        <Image x:Name="PauseImage" Width="16" Height="16" Opacity="0.85"
                                               RenderOptions.BitmapScalingMode="HighQuality"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="PauseImage" Property="Opacity" Value="1.0"/></Trigger>
                                        <Trigger Property="IsPressed" Value="True"><Setter TargetName="PauseImage" Property="Opacity" Value="0.5"/></Trigger>
                                        <Trigger Property="IsEnabled" Value="False"><Setter TargetName="PauseImage" Property="Opacity" Value="0.25"/></Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button x:Name="ResumeButton" Width="24" Height="24" Padding="0" Margin="0.2,0,0.2,0"
                                FocusVisualStyle="{x:Null}" Click="ResumeButton_Click" IsEnabled="False">
                            <Button.ToolTip><ToolTip x:Name="ResumeTooltip"/></Button.ToolTip>
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="Transparent" HorizontalAlignment="Center" VerticalAlignment="Stretch">
                                        <Image x:Name="ResumeImage" Width="16" Height="16" Opacity="0.85"
                                               RenderOptions.BitmapScalingMode="HighQuality"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ResumeImage" Property="Opacity" Value="1.0"/></Trigger>
                                        <Trigger Property="IsPressed" Value="True"><Setter TargetName="ResumeImage" Property="Opacity" Value="0.5"/></Trigger>
                                        <Trigger Property="IsEnabled" Value="False"><Setter TargetName="ResumeImage" Property="Opacity" Value="0.25"/></Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button x:Name="StopButton" Width="24" Height="24" Padding="0" Margin="0.2,0,0.2,0"
                                FocusVisualStyle="{x:Null}" Click="StopButton_Click" IsEnabled="False">
                            <Button.ToolTip><ToolTip x:Name="StopTooltip"/></Button.ToolTip>
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="Transparent" HorizontalAlignment="Center" VerticalAlignment="Stretch">
                                        <Image x:Name="StopImage" Width="16" Height="16" Opacity="0.85"
                                               RenderOptions.BitmapScalingMode="HighQuality"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="StopImage" Property="Opacity" Value="1.0"/></Trigger>
                                        <Trigger Property="IsPressed" Value="True"><Setter TargetName="StopImage" Property="Opacity" Value="0.5"/></Trigger>
                                        <Trigger Property="IsEnabled" Value="False"><Setter TargetName="StopImage" Property="Opacity" Value="0.25"/></Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button x:Name="DropButton" Width="24" Height="24" Padding="0" Margin="0.2,0,0.2,0"
                                FocusVisualStyle="{x:Null}" Click="DropButton_Click" IsEnabled="False">
                            <Button.ToolTip><ToolTip x:Name="DropTooltip"/></Button.ToolTip>
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="Transparent" HorizontalAlignment="Center" VerticalAlignment="Stretch">
                                        <Image x:Name="DropImage" Width="16" Height="16" Opacity="0.85"
                                               RenderOptions.BitmapScalingMode="HighQuality"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="DropImage" Property="Opacity" Value="1.0"/></Trigger>
                                        <Trigger Property="IsPressed" Value="True"><Setter TargetName="DropImage" Property="Opacity" Value="0.5"/></Trigger>
                                        <Trigger Property="IsEnabled" Value="False"><Setter TargetName="DropImage" Property="Opacity" Value="0.25"/></Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button x:Name="HideButton" Width="24" Height="24" Padding="0" Margin="0.2,0,0.2,0"
                                FocusVisualStyle="{x:Null}" Click="HideButton_Click">
                            <Button.ToolTip><ToolTip x:Name="HideTooltip"/></Button.ToolTip>
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="Transparent" HorizontalAlignment="Center" VerticalAlignment="Stretch">
                                        <Image x:Name="HideImage" Width="16" Height="16" Opacity="0.85"
                                               RenderOptions.BitmapScalingMode="HighQuality"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="HideImage" Property="Opacity" Value="1.0"/></Trigger>
                                        <Trigger Property="IsPressed" Value="True"><Setter TargetName="HideImage" Property="Opacity" Value="0.5"/></Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Border>

                <!-- 信息行：内层高透明背景，点击打开录音目录 -->
                <Border Background="#55252525" CornerRadius="3" Height="22" Padding="6,3"
                        Margin="0,0,0,3" Cursor="Hand" MouseLeftButtonDown="InfoBox_Click"
                        HorizontalAlignment="Center" Width="125">
                    <Border.ToolTip><ToolTip x:Name="InfoTooltip"/></Border.ToolTip>
                    <TextBlock x:Name="InfoTextBlock" Text="00:00 | 0 KB"
                               Foreground="#F5F5F5" FontSize="9" FontFamily="Consolas"
                               TextAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Border>

                <!-- 设备信息行：内层高透明背景，文字居左，鼠标悬停 tooltip 显示全名 -->
                <Border x:Name="DeviceInfoBorder" Background="#55252525" CornerRadius="3"
                        Padding="6,3" Margin="0,0,0,0" Visibility="Collapsed"
                        HorizontalAlignment="Center" Width="125">
                    <StackPanel>
                        <TextBlock x:Name="MicDeviceText"
                                   Foreground="#F5F5F5" FontSize="8" FontFamily="Consolas"
                                   TextTrimming="CharacterEllipsis" TextAlignment="Left"
                                   HorizontalAlignment="Stretch" Visibility="Collapsed"/>
                        <TextBlock x:Name="SpeakerDeviceText"
                                   Foreground="#F5F5F5" FontSize="8" FontFamily="Consolas"
                                   TextTrimming="CharacterEllipsis" TextAlignment="Left"
                                   HorizontalAlignment="Stretch" Visibility="Collapsed"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </Border>
    </StackPanel>
    </Grid>
</Window>

